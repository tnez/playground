{
  "name": "components",
  "version": "0.1.0",
  "description": "Shared components",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "dev": "start-storybook",
    "lint": "eslint ./**/*.js --color",
    "now-build": "rm -rf .out && build-storybook -c .storybook -o .out",
    "now-dev": "start-storybook -p $PORT",
    "test": "echo \"Error: run tests from root\" && exit 1"
  },
  "dependencies": {
    "@material-ui/core": "^4.0.2",
    "@material-ui/icons": "^4.0.1",
    "@material-ui/styles": "^4.0.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "devDependencies": {
    "@babel/core": "^7.4.5",
    "@storybook/addon-knobs": "^5.1.1",
    "@storybook/react": "^5.1.1",
    "babel-loader": "^8.0.6",
    "webpack": "^4.33.0"
  }
}
